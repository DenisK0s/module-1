'use strict'

// Определить площадь круга с радиусом 10см (ПR(2)). Math.pow(a, b) - возведение в степень

const radius = 10;

const aria = Math.PI * Math.pow(radius, 2);

const aria = Math.PI * radius ** 2; // только если возводить в квадрат

// const raiseToPow = (number, n) => ;

